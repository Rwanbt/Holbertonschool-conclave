# URL publique du backend CONCLAVE (aucun secret ici : les clés LLM sont BYOK
# côté navigateur et ne doivent JAMAIS apparaître dans un VITE_*).
#
# Local :    http://localhost:8000
# Production (frontend Netlify -> HTTPS direct -> backend ASGI) :
#   VITE_API_BASE_URL=https://api.conclave.example
VITE_API_BASE_URL=http://localhost:8000